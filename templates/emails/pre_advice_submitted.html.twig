<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>FREE-ADVICE Request Submitted</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #2c3e50; color: white; padding: 20px; text-align: center; }
        .content { padding: 20px; background-color: #f8f9fa; }
        .details { background-color: white; padding: 15px; margin: 10px 0; border-radius: 5px; }
        .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
        .status { padding: 5px 10px; border-radius: 3px; font-weight: bold; }
        .status.pending { background-color: #ffc107; color: #212529; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>FREE-ADVICE Request Submitted</h1>
        </div>
        
        <div class="content">
            <p>Dear {{ trucker.fullName }},</p>
            
            <p>Your FREE-ADVICE request has been successfully submitted and is now pending verification by our Terminal Team.</p>
            
            <div class="details">
                <h3>Request Details:</h3>
                <p><strong>Reference Number:</strong> {{ preAdviceRequest.id }}</p>
                <p><strong>Container Number:</strong> {{ preAdviceRequest.container.containerNumber }}</p>
                <p><strong>Selected Terminal:</strong> {{ preAdviceRequest.selectedTerminal.name }} ({{ preAdviceRequest.selectedTerminal.type.value }})</p>
                <p><strong>Status:</strong> <span class="status pending">{{ preAdviceRequest.status.value|title }}</span></p>
                <p><strong>Submitted:</strong> {{ preAdviceRequest.createdAt|date('Y-m-d H:i:s') }}</p>
                {% if preAdviceRequest.paymentReference %}
                <p><strong>Payment Reference:</strong> {{ preAdviceRequest.paymentReference }}</p>
                {% endif %}
            </div>
            
            <div class="details">
                <h3>What's Next?</h3>
                <ul>
                    <li>Our Terminal Team will review your request and uploaded photos</li>
                    <li>You will receive an email notification once the verification is complete</li>
                    <li>If approved, you will receive your EDO and QR code for terminal access</li>
                    <li>If rejected, you will receive the reason and can submit a new request</li>
                </ul>
            </div>
            
            <p>You can track the status of your request by logging into your trucker dashboard.</p>
            
            <p>Thank you for using OPTIMUS Terminal FREE-ADVICE System.</p>
        </div>
        
        <div class="footer">
            <p>This is an automated message from OPTIMUS Shipping Portal.<br>
            Please do not reply to this email.</p>
        </div>
    </div>
</body>
</html>