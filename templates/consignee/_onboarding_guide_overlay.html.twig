<div id="consigneeGuideOverlay" class="hidden fixed inset-0 z-[999999] pointer-events-none" aria-hidden="true">
    <div id="consigneeGuidePanelTop" class="fixed bg-black/60 backdrop-blur-md pointer-events-auto"></div>
    <div id="consigneeGuidePanelLeft" class="fixed bg-black/60 backdrop-blur-md pointer-events-auto"></div>
    <div id="consigneeGuidePanelRight" class="fixed bg-black/60 backdrop-blur-md pointer-events-auto"></div>
    <div id="consigneeGuidePanelBottom" class="fixed bg-black/60 backdrop-blur-md pointer-events-auto"></div>

    <div id="consigneeGuideHighlight" class="fixed z-[1000001] pointer-events-none rounded-xl ring-4 ring-primary/80 ring-offset-2 ring-offset-base-200"></div>

    <div id="consigneeGuideTooltip" class="fixed z-[1000002] w-[min(calc(100vw-2rem),22rem)] rounded-2xl bg-base-100 shadow-2xl border border-base-300 p-5 pointer-events-auto">
        <p id="consigneeGuideStepLabel" class="text-xs font-semibold uppercase tracking-wide text-primary">Step 1</p>
        <h3 id="consigneeGuideTooltipTitle" class="mt-1 text-lg font-bold text-base-content"></h3>
        <p id="consigneeGuideTooltipText" class="mt-2 text-sm text-base-content/70"></p>
        <div class="mt-4 flex items-center justify-end gap-2">
            <button type="button" id="consigneeGuideSkipBtn" class="btn btn-ghost btn-sm">Skip</button>
            <button type="button" id="consigneeGuideGotItBtn" class="btn btn-primary btn-sm">Got it</button>
        </div>
    </div>
</div>
