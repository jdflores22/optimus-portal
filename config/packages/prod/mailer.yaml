framework:
    mailer:
        # Production configuration - use real SMTP
        dsn: '%env(MAILER_DSN)%'
        headers:
            from: '%env(MAILER_FROM_EMAIL)%'
        # Enable delivery in production
        envelope:
            sender: '%env(MAILER_FROM_EMAIL)%'