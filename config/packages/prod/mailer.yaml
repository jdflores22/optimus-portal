framework:
    mailer:
        # Send immediately on Hostinger — no background worker required
        message_bus: false
        dsn: '%env(MAILER_DSN)%'
        headers:
            from: '%env(MAILER_FROM_EMAIL)%'
        envelope:
            sender: '%env(MAILER_FROM_EMAIL)%'
