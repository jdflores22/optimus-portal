framework:
    cache:
        prefix_seed: optimus_shipping_portal
        app: cache.adapter.filesystem

        pools:
            avatar_colors.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 3600
            form_configurations.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 3600
            user_permissions.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 1800
            dashboard_metrics.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 300
            file_metadata.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 7200
            manifest_workflow.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 3600
            manifest_authorization.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 1800
            edo_pdf.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 86400
            payment_history.cache:
                adapter: cache.adapter.filesystem
                default_lifetime: 300

when@prod:
    framework:
        cache:
            # Use filesystem cache in production (Redis not available on Hostinger)
            app: cache.adapter.filesystem
            # default_redis_provider: '%env(REDIS_URL)%'
            pools:
                avatar_colors.cache:
                    adapter: cache.adapter.filesystem
                form_configurations.cache:
                    adapter: cache.adapter.filesystem
                user_permissions.cache:
                    adapter: cache.adapter.filesystem
                dashboard_metrics.cache:
                    adapter: cache.adapter.filesystem
                file_metadata.cache:
                    adapter: cache.adapter.filesystem
                manifest_workflow.cache:
                    adapter: cache.adapter.filesystem
                manifest_authorization.cache:
                    adapter: cache.adapter.filesystem
                edo_pdf.cache:
                    adapter: cache.adapter.filesystem
                payment_history.cache:
                    adapter: cache.adapter.filesystem
